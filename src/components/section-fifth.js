import './section-fifth.less'
import React from 'react'

const SectionFifth = ({ children, id }) => {
  return (
    <section className="section-fifth" id={id}>
      {children}
    </section>
  )
}

export default SectionFifth
